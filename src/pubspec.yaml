name: counter_app
description: A simple counter app built using Flutter and the Cubit pattern for state management.
version: 1.0.0
environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0
  bloc: ^8.0.0
  equatable: ^2.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.7
  mocktail: ^0.2.0
flutter:
  uses-material-design: true